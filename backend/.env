PORT=5000
NODE_ENV=development
JWT_SECRET=famehub-enterprise-secret-key-2026-xyz
JWT_EXPIRES_IN=7d

# Database Configuration
# PostgreSQL (default)
DATABASE_URL=postgresql://postgres.wgddawhvbwgrouxakhwh:LN0aZsM2SRc6oxuF@aws-1-ap-south-1.pooler.supabase.com:5432/postgres
# Fallback SQLite DB file if postgres fails to connect
SQLITE_DB_PATH=./database.sqlite

# Caching Configuration
REDIS_URL=redis://localhost:6379

# Kafka Configuration
KAFKA_CLIENT_ID=famehub-lms
KAFKA_BROKERS=localhost:9092

# BigBlueButton Configuration
BBB_URL=https://app.bbbserver.com/bbb-integration-v2/60a53a74-f9df-496a-9ae1-ca3079f26a7c/api
BBB_SECRET=118c788f-d02e-444a-bbbe-055235f0100b

# Attendance Configuration
# Duration in seconds a student must stay to mark them 'Present'
ATTENDANCE_THRESHOLD_SECONDS=60
